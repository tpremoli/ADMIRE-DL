task_name: tl_ax_vgg19_maxpooling_SGD
dataset: out/preprocessed_datasets/adni_processed/axial_dataset
options:
    architecture: VGG19
    method: transferlearn

    pooling: max # This has given better results (image size)
    epochs: 100
    batch_size: 32
    overrides:
        optimizer_name: SGD # This has given better results